/** Mindly_new — 앱·SW 버전 (단일 소스). 배포 시 함께 올려주세요. */
export const APP_VERSION = '3.0.4';
export const SW_CACHE_VERSION = 'mindly-v3.0.4';

export function getVersionLabel(): string {
  return `v${APP_VERSION}`;
}
